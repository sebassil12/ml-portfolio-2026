The dataset source url is https://www.kaggle.com/datasets/amanalisiddiqui/fraud-detection-dataset?resource=download

The code takes the data that is inside in a folder called data
Do not upload the dataset because the size of the file
